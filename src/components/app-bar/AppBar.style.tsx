import styled from 'styled-components'

export const Wrapper = styled.div`
  background-color: var(--primary);
  width: 100vw;
  height: 10rem;
  display: flex;
  align-items: center;
  padding: 0 2rem;

  position: fixed;
  top: 0;
`
