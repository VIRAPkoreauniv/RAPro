import styled from 'styled-components'

const Logo = () => {
  return <Text>RAPro</Text>
}

export default Logo

const Text = styled.span`
  color: var(--white, #f7f7f7);
  font-size: 4rem;
  font-style: italic;
  font-weight: 800;
  line-height: normal;
`
